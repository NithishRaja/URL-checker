# Testing code for master class

Code for node.js master class without any framework can be found here

## Start up
* create a `/.data` directory at root path

## Editing code
* route logic is found inside `/router` directory
